# Este archivo es un template. El secret real será creado por GitHub Actions
# con el connection string correcto durante el deployment
apiVersion: v1
kind: Secret
metadata:
  name: sql-connection-secret
  namespace: inventory-system
type: Opaque
stringData:
  connection-string: "PLACEHOLDER_WILL_BE_REPLACED_BY_CICD"
